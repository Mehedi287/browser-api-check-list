const seeUrl = () => {

    const href = 'https//www.google.com';
    const con = confirm(href);
    if (href) {
        console.log(href);
    }
    else {
        console.log('you cannot see the url');
    }
}
